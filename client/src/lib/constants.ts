// Solar System scales (not to real scale, but relatively accurate for visualization)
export const SCALE_FACTOR = 1.0; // Adjust this to scale the entire system

// Colors matching the design system
export const COLORS = {
  BLACK: "#000000", // space black
  NAVY: "#1A237E", // deep navy
  BLUE: "#4FC3F7", // star blue
  ORANGE: "#FFA726", // solar orange
  GREY: "#424242", // space grey
};

// Camera settings
export const DEFAULT_CAMERA_POSITION = [0, 30, 100];
export const MIN_CAMERA_DISTANCE = 10;
export const MAX_CAMERA_DISTANCE = 200;

// Default values for celestial body properties
export const DEFAULT_ROTATION_SPEED = 0.001;
export const DEFAULT_ORBIT_SPEED = 0.0005;
